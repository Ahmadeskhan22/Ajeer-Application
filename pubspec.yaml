name: ajeer
description: AJEER - Job Seeker App (أجير - تطبيق الباحثين عن عمل)
version: 1.0.0+1
publish_to: none


environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.6.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  image_picker: ^1.0.7

  provider: ^6.1.2
  shared_preferences: ^2.5.5
  google_fonts: ^6.2.1
  go_router: ^13.2.4
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
